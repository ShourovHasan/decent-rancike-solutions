import React from 'react';
import useTitle from '../../hooks/useTitle';

const Customers = () => {
    useTitle('Customers');
    return (
        <div className='text-2xl text-center lg:text-3xl'>
            Customers Page Coming Soon
        </div>
    );
};

export default Customers;