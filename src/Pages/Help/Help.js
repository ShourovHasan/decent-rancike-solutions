import React from 'react';
import useTitle from '../../hooks/useTitle';

const Help = () => {
    useTitle('Need help');
    return (
        <div className='text-2xl text-center lg:text-3xl'>
            Need help Page Coming Soon
        </div>
    );
};

export default Help;